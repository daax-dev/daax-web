{"id":"ws-basepath-mount-confinement-001","date":"2026-08-17","decision":"Always serialize the workspace basePath on every terminal WS URL, not only when a project is selected","rationale":"buildAIWsUrl() set the basePath param inside the if(options.projectName) block although createAISession() always threads the value in; a no-project /ai-coding session therefore sent mount=~/jarvis with no basePath, the server fell back to its hardcoded ~/prj default, and every AI session died with close 1008 Path not allowed. buildWsUrl()'s claude branch had the identical omission","alternatives":"Fix only the server default (leaves the client sending incomplete state and the same trap for any other consumer); change the server default to derive from HOST_WORKSPACE_PATH only (does not help host mode and still relies on an implicit convention)","references":[".logs/decisions/ws-basepath-mount-confinement.md","components/terminal/TerminalManager.tsx"]}
{"id":"ws-basepath-mount-confinement-002","date":"2026-08-17","decision":"Add tildeToHostWorkspace() and map ~/<basename(HOST_WORKSPACE_PATH)> onto the host workspace root in the WS mount resolver, independent of the client-supplied basePath","rationale":"In container mode expandPath() resolves ~ against the CONTAINER home (/home/node), so a tilde mount that does not match basePath produced /home/node/jarvis — a container path used as a HOST bind-mount source that translatePath() cannot map into /workspace, so the #186 confinement check could only ever reject it. The hardcoded ~/prj default guarantees this on any deployment whose workspace basename differs, and a client-only fix leaves every stale browser tab broken","alternatives":"Relax the #186 confinement check (rejected: the check is correct, the path handed to it was wrong); default basePathParam to the workspace basename (rejected: same implicit convention, but silently mis-resolves any other tilde path)","references":["server/handlers/connection-handler.ts","lib/path-utils.ts","lib/worktree-manager.ts"]}
{"id":"ws-basepath-mount-confinement-003","date":"2026-08-17","decision":"tildeToHostWorkspace() takes the workspace root as a parameter instead of reading HOST_WORKSPACE_PATH or homedir()","rationale":"Keeps the helper pure so its tests cannot collapse the host and container home namespaces into one value — the mocking failure mode that previously hid a /workspace-vs-~/prj mismatch in this same code. The test suite pins a workspace root outside any home directory for the same reason","alternatives":"Keep the helper module-local in connection-handler.ts reading the HOST_WORKSPACE_PATH const (rejected: only testable by importing the whole WS handler with its PTY/docker/recorder dependencies, and env-const timing forces resetModules gymnastics)","references":["tests/server/tilde-workspace-mount.test.ts","tests/terminal-ws-basepath.test.tsx"]}
{"id":"ws-basepath-mount-confinement-004","date":"2026-08-17","decision":"Recreate the live galway daax container on a locally built fix image, preserving the existing single-container topology rather than switching to the split web/terminal deploy compose","rationale":"The running container was created outside compose (no compose labels) and runs both planes; the repo deploy/docker-compose.yml is now the split F3 topology. Changing topology while fixing an outage would confound the fix with an unrelated migration. The previous container is retained stopped as daax-prev-fixbasepath and the prior image tagged ghcr.io/daax-dev/daax-web:rollback-20260817 for rollback","alternatives":"Wait for a merge to main and the GHCR publish (rejected: leaves the site broken and only humans may merge); docker compose -p daax -f deploy/docker-compose.yml up --build (rejected: silently changes the deployed topology)","references":[".logs/decisions/ws-basepath-mount-confinement.md"]}
{"id":"ws-basepath-mount-confinement-005","date":"2026-08-17","decision":"Start the pre-existing daax-postgres container, which had sat in Created state since 2026-07-27","rationale":"/api/health returned db:false and the app container had 20746 consecutive failing health checks. The data directory on daax_daax-pg-data already contained an initialized PG18 cluster, so starting it skips initdb and is reversible with docker stop; health now returns ok/db:true. Unrelated to the WS bug — Traefik does not gate on health, so the outage was reached independently","alternatives":"Leave it and only report (rejected: the app was running degraded with its catalog/releases/audit store unreachable); recreate via compose up (rejected: would recreate sibling services and risk a wider change during an outage fix)","references":["/opt/daax/docker-compose.yml"]}
{"id":"ws-basepath-mount-confinement-006","date":"2026-08-17","decision":"Route Herdr-backed sessions to the Full Bundle agent image via resolveHerdrImage() instead of the configured default","rationale":"herdr is installed ONLY in the daax-agents Full Bundle; all four other published variants lack it, including daax-agents-gsd which is the UI default and most-spawned image. Selecting 'Herdr + Claude' therefore always died on the pre-flight 'Herdr is not installed in this container image.' The matcher uses a negative lookahead on '-' so a locally built or retagged Full Bundle (daax-agents:claude-current) is preserved and an operator override is not undone","alternatives":"Add herdr to every variant (rejected: the images are built in a different repo, peregrinesummit/daax, and this cannot be fixed from daax-web); hide the UI option unless the Full Bundle is selected (rejected: silently removes a working feature instead of making it work)","references":["lib/settings.ts","components/terminal/TerminalManager.tsx","tests/herdr-session.test.tsx"]}
{"id":"ws-basepath-mount-confinement-007","date":"2026-08-17","decision":"Run the herdr-claude bootstrap as the container's own command rather than typing it into an already-running shell","rationale":"buildFullCommand('herdr-claude') expands to a ~1020-character multi-stage pipeline. scheduleCommand() wrote it into the PTY, so the shell echoed the entire wall of text at the operator before anything ran. Passing it as the zsh -c argument produces the same result with nothing echoed — verified live: 0 occurrences of the bootstrap text on screen, output begins with herdr's TUI init sequences. Gated behind isInlineBootstrapCommand() whose contract is that the expansion ends by exec'ing a long-running process, since a container command that returns would exit the container","alternatives":"stty -echo around the write (rejected: fragile, and the disable itself races the echo); clear the screen afterwards (rejected: the wall of text still flashes); apply to every AI tool (rejected: their commands are short and the interactive-shell fallback is load-bearing for them)","references":["server/handlers/command-handler.ts","server/handlers/connection-handler.ts"]}
